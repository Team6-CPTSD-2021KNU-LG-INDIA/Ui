"use strict";

var _enzyme = require("enzyme");

var _FixedPopupPanels = _interopRequireDefault(require("../FixedPopupPanels"));

var _jsxRuntime = require("react/jsx-runtime");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

describe('FixedPopupPanels', function () {
  it('should have the default width when nothing is assigned', function () {
    var subject = (0, _enzyme.mount)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_FixedPopupPanels["default"], {
      open: true
    }));
    var expected = 'narrow';
    var actual = subject.find('Popup').prop('className');
    expect(actual).toContain(expected);
  });
  it('should have narrow width applied when width="narrow"', function () {
    var subject = (0, _enzyme.mount)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_FixedPopupPanels["default"], {
      width: "narrow",
      open: true
    }));
    var expected = 'narrow';
    var actual = subject.find('Popup').prop('className');
    expect(actual).toContain(expected);
  });
  it('should have half width applied when width="half"', function () {
    var subject = (0, _enzyme.mount)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_FixedPopupPanels["default"], {
      width: "half",
      open: true
    }));
    var expected = 'half';
    var actual = subject.find('Popup').prop('className');
    expect(actual).toContain(expected);
  });
  it('should correctly assign the fullHeight class', function () {
    var subject = (0, _enzyme.mount)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_FixedPopupPanels["default"], {
      fullHeight: true,
      open: true
    }));
    var expected = 'fullHeight';
    var actual = subject.find('Popup').prop('className');
    expect(actual).toContain(expected);
  });
});