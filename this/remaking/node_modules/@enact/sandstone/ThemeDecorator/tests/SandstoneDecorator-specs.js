"use strict";

var _enzyme = require("enzyme");

var _ = _interopRequireDefault(require("../"));

var _spotlight = _interopRequireDefault(require("@enact/spotlight"));

var _ThemeDecoratorModule = _interopRequireDefault(require("../ThemeDecorator.module.css"));

var _jsxRuntime = require("react/jsx-runtime");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); if (enumerableOnly) symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; }); keys.push.apply(keys, symbols); } return keys; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; if (i % 2) { ownKeys(Object(source), true).forEach(function (key) { _defineProperty(target, key, source[key]); }); } else if (Object.getOwnPropertyDescriptors) { Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)); } else { ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

describe('ThemeDecorator', function () {
  var AppRoot = function AppRoot(props) {
    return /*#__PURE__*/(0, _jsxRuntime.jsx)("div", _objectSpread({
      "data-app": true
    }, props));
  };

  var a11yConfig = {
    "float": false,
    ri18n: false,
    ri: false,
    spotlight: false
  };
  var A11yApp = (0, _["default"])(a11yConfig, AppRoot);
  test('should add base classes to wrapped component', function () {
    var config = {
      ri: false,
      i18n: false,
      spotlight: false,
      "float": false,
      overlay: false
    };
    var App = (0, _["default"])(config, AppRoot);
    var subject = (0, _enzyme.mount)( /*#__PURE__*/(0, _jsxRuntime.jsx)(App, {}));

    _spotlight["default"].terminate();

    var appRoot = subject.find('[data-app]');
    var expected = true;
    var actual = appRoot.hasClass('neutral') && appRoot.hasClass(_ThemeDecoratorModule["default"].bg);
    expect(actual).toBe(expected);
  });
  test('should add author classes to wrapped component', function () {
    var config = {
      ri: false,
      i18n: false,
      spotlight: false,
      "float": false,
      overlay: false
    };
    var App = (0, _["default"])(config, AppRoot);
    var subject = (0, _enzyme.mount)( /*#__PURE__*/(0, _jsxRuntime.jsx)(App, {
      className: "author-class"
    }));

    _spotlight["default"].terminate();

    var appRoot = subject.find('[data-app]');
    var expected = true;
    var actual = appRoot.hasClass('author-class');
    expect(actual).toBe(expected);
  });
  test('should not add skin classname to wrapped component when float is enabled', function () {
    var config = {
      ri: false,
      i18n: false,
      spotlight: false,
      "float": true,
      overlay: false
    };
    var App = (0, _["default"])(config, AppRoot);
    var subject = (0, _enzyme.mount)( /*#__PURE__*/(0, _jsxRuntime.jsx)(App, {}));

    _spotlight["default"].terminate();

    var appRoot = subject.find('[data-app]');
    var expected = false;
    var actual = appRoot.hasClass('neutral');
    expect(actual).toBe(expected);
  });
  test('should not add .bg class to wrapped component when overlay is enabled', function () {
    var config = {
      ri: false,
      i18n: false,
      spotlight: false,
      "float": false,
      overlay: true
    };
    var App = (0, _["default"])(config, AppRoot);
    var subject = (0, _enzyme.mount)( /*#__PURE__*/(0, _jsxRuntime.jsx)(App, {}));

    _spotlight["default"].terminate();

    var appRoot = subject.find('[data-app]');
    var expected = false;
    var actual = appRoot.hasClass(_ThemeDecoratorModule["default"].bg);
    expect(actual).toBe(expected);
  });
  describe('AccessibilityDecorator', function () {
    test('should add the highContrast class if passing the highContrast prop of true', function () {
      var subject = (0, _enzyme.mount)( /*#__PURE__*/(0, _jsxRuntime.jsx)(A11yApp, {
        highContrast: true
      }));
      var appRoot = subject.find('[data-app]');
      var expected = true;
      var actual = appRoot.hasClass('highContrast');
      expect(actual).toBe(expected);
    });
    test('should add the largeText class if passing the textSize prop of `large`', function () {
      var subject = (0, _enzyme.mount)( /*#__PURE__*/(0, _jsxRuntime.jsx)(A11yApp, {
        textSize: "large"
      }));
      var appRoot = subject.find('[data-app]');
      var expected = true;
      var actual = appRoot.hasClass('largeText');
      expect(actual).toBe(expected);
    });
    test('should update the skinVariants based on the skinVariants prop', function () {
      var subject = (0, _enzyme.mount)( /*#__PURE__*/(0, _jsxRuntime.jsx)(A11yApp, {
        skinVariants: ['grayscale']
      }));
      var appRoot = subject.find('[data-app]');
      var expected = true;
      var actual = appRoot.hasClass('grayscale');
      expect(actual).toBe(expected);
    });
    test('should add the largeText class when the type of the skinVariants prop is a string', function () {
      var subject = (0, _enzyme.mount)( /*#__PURE__*/(0, _jsxRuntime.jsx)(A11yApp, {
        textSize: "large",
        skinVariants: "grayscale"
      }));
      var appRoot = subject.find('[data-app]');
      var expected = true;
      var actual = appRoot.hasClass('largeText') && appRoot.hasClass('grayscale');
      expect(actual).toBe(expected);
    });
    test('should add largeText class when the type of the skinVariants prop is an array', function () {
      var subject = (0, _enzyme.mount)( /*#__PURE__*/(0, _jsxRuntime.jsx)(A11yApp, {
        textSize: "large",
        skinVariants: ['grayscale']
      }));
      var appRoot = subject.find('[data-app]');
      var expected = true;
      var actual = appRoot.hasClass('largeText') && appRoot.hasClass('grayscale');
      expect(actual).toBe(expected);
    });
    test('should add largeText class when the type of the skinVariants prop is an object', function () {
      var subject = (0, _enzyme.mount)( /*#__PURE__*/(0, _jsxRuntime.jsx)(A11yApp, {
        textSize: "large",
        skinVariants: {
          grayscale: true
        }
      }));
      var appRoot = subject.find('[data-app]');
      var expected = true;
      var actual = appRoot.hasClass('largeText') && appRoot.hasClass('grayscale');
      expect(actual).toBe(expected);
    });
  });
});