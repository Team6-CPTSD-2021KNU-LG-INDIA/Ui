"use strict";

var _enzyme = require("enzyme");

var _ProgressButton = require("../ProgressButton");

var _jsxRuntime = require("react/jsx-runtime");

describe('ProgressButton Specs', function () {
  test('should show text', function () {
    var expected = 'Progress Button';
    var progressButton = (0, _enzyme.shallow)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_ProgressButton.ProgressButtonBase, {
      children: "Progress Button"
    }));
    var actual = progressButton.prop('children');
    expect(actual).toBe(expected);
  });
  test('should have a root className `progressButton`', function () {
    var expected = 'progressButton';
    var progressButton = (0, _enzyme.shallow)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_ProgressButton.ProgressButtonBase, {
      children: "Progress Button"
    }));
    var actual = progressButton.prop('className');
    expect(actual).toBe(expected);
  });
  test('should show radial progress', function () {
    var expected = 'radial';
    var progressButton = (0, _enzyme.shallow)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_ProgressButton.ProgressButtonBase, {
      showProgress: true,
      children: "Progress Button"
    }));
    var actual = progressButton.find('.progressContainer').childAt(0).prop('orientation');
    expect(actual).toBe(expected);
  });
  test('should show 0.5 progress', function () {
    var expected = 0.5;
    var progressButton = (0, _enzyme.shallow)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_ProgressButton.ProgressButtonBase, {
      showProgress: true,
      progress: 0.5,
      children: "Progress Button"
    }));
    var actual = progressButton.find('.progressContainer').childAt(0).prop('progress');
    expect(actual).toBe(expected);
  });
  test('should show default icon `stop`', function () {
    var expected = 'stop';
    var progressButton = (0, _enzyme.shallow)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_ProgressButton.ProgressButtonBase, {
      showProgress: true,
      progress: 0.5,
      children: "Progress Button"
    }));
    var actual = progressButton.find('.icon').prop('children');
    expect(actual).toBe(expected);
  });
  test('should support a custom icon', function () {
    var expected = 'play';
    var progressButton = (0, _enzyme.shallow)( /*#__PURE__*/(0, _jsxRuntime.jsx)(_ProgressButton.ProgressButtonBase, {
      showProgress: true,
      progress: 0.5,
      icon: expected,
      children: "Progress Button"
    }));
    var actual = progressButton.find('.icon').prop('children');
    expect(actual).toBe(expected);
  });
});